const accountId = 14553
let accountEmail = "luqman@google.com"
var accountPassword = "12345"
accountCity = "lahore"

// JS me agar ksi variable ko declare kr k chor dete ha aur koi value ni dete to uska result undefined hoga
let accountState;





// accountId = 2  // not allowed because we ave already defined this values which is "14553". If we use const varable, it means that we cant change it.

accountEmail = "luqman@yopmail.com"
accountPassword = "123"
accountCity = "ATD"

// console.log(accountId);
// console.log([accountId, accountEmail, accountPassword, accountCity]);

// is se hme ek tabular structure me outpu milyga
console.table([accountId, accountEmail, accountPassword, accountCity, accountState]); 

/* 
    Prefer not to use var becasue of isseu in block scope and functional scope
*/
